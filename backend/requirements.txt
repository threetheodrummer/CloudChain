# Minimum versions rather than exact pins, so pip resolves builds that have
# wheels for your Python version (important on 3.13/3.14, where older pinned
# pydantic releases have no prebuilt wheel and fail to compile from source).
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
boto3>=1.35.0
networkx>=3.3
pydantic>=2.9.0
python-dateutil>=2.9.0
pytest>=8.3.2
httpx>=0.27.2
